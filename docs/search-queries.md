### 1. Intersección General

Busca la coincidencia exacta entre la técnica, el tipo de dato y el campo de aplicación.

```text
("passive acoustic monitoring" OR "bioacoustics" OR "soundscape ecology") 
AND 
("deep learning" OR "convolutional neural networks" OR "CNN" OR "U-Net") 
AND 
("sound event detection" OR "spectrogram segmentation" OR "semantic segmentation")

```

### 2. Enfoque Metodológico

Centrado en la técnica de segmentación fina, independientemente de la especie.

```text
("U-Net" OR "attention U-Net" OR "pixel-wise classification") 
AND 
("spectrogram" OR "audio signal processing") 
AND 
("bioacoustics" OR "animal vocalization")

```

### 3. Enfoque Específico

Para clasificación de especies, individuos y secuencias temporales.

```text
("primates" OR "monkeys" OR "anthropology") 
AND 
("vocalization classification" OR "individual recognition" OR "identity signaling") 
AND 
("deep learning" OR "LSTM" OR "BiLSTM" OR "recurrent neural network")

```

### 4. Escenarios de Datos Limitados

Para solucionar problemas de pocos datos anotados y falsos positivos en campo abierto.

```text
("transfer learning" OR "few-shot learning" OR "open-set recognition") 
AND 
("bioacoustics" OR "sound event detection") 
AND 
("noise robustness" OR "false positive reduction")

```