"""Lo que comparten los notebooks de inspección (`notebooks/`): un split del caché en memoria
(`windows`), ventanas dibujadas con sus cajas (`figures`) y predicciones frente a anotaciones
(`predictions`)."""
