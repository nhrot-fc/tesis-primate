Each model is a subfolder of models\ with its checkpoint and operating point.

To add one, drag its zip (detector-*-model-*.zip) onto Detector.exe, or use
"Add model from zip..." in the Model list. It is the same as unzipping it here.
